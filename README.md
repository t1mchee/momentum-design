# Momentum reversal-risk system, design canvas

Two views of one spec. `index.html` is the full graph; `levels.html` is the same system in seven steps that open into their sub-steps. `spec.yaml` is the source of truth, `groups.yaml` the hierarchy, `changelog.yaml` the history; `versions/` holds every snapshot.
